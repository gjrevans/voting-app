var ApiModel = function(){};

ApiModel.prototype.index = function(options, callback){
    /* Example Model Function */

    /*
    var query = options.query,
        limit = options.limit,
        offset = options.offset;

    callback(false, someValue);
    */
};

module.exports = ApiModel;
